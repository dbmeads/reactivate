{
  "name": "reactivate",
  "version": "1.1.0",
  "description": "React'ivate",
  "main": "dist/index.js",
  "author": "dmeads@inclyn.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/Inclyn/reactivate.git"
  },
  "scripts": {
    "babel": "babel src/lib -d lib && babel src/test -d test",
    "build": "npm run clean && npm run babel",
    "clean": "rm -rf lib && rm -rf test",
    "mocha": "mocha",
    "test": "npm run build && npm run mocha"
  },
  "dependencies": {
    "tv4": "^1.2.7"
  },
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-preset-es2015": "^6.6.0",
    "chai": "^3.5.0",
    "mocha": "^2.4.5"
  }
}
